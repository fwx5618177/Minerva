import { Avatar, AvatarFallback, AvatarImage } from "./Avatar";
import type {
  AvatarProps,
  AvatarFallbackProps,
  AvatarImageProps,
} from "./types";

export {
  Avatar,
  AvatarFallback,
  AvatarImage,
  AvatarProps,
  AvatarFallbackProps,
  AvatarImageProps,
};

export default Avatar;
